{
  "name": "sprint-tracker-root",
  "private": true,
  "workspaces": [
    "react-frontend"
  ],
  "scripts": {
    "dev": "npm run --prefix react-frontend dev --host 0.0.0.0 --port 5173",
    "build": "npm run --prefix react-frontend build",
    "lint": "npm run --prefix react-frontend lint"
  }
} 